services:
  wallplayer:
    image: ghcr.io/jkerdreux-imt/wallplayer:latest
    ports:
      - "9999:9999"
    volumes:
      - ./videos:/videos:ro
      - wallplayer_data:/data
    environment:
      - PORT=9999
      - VIDEOS_DIR=/videos
    restart: unless-stopped

volumes:
  wallplayer_data:
